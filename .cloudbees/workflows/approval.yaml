apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: approval-test
on:
  push:
    branches:
      - "**"
  workflow_dispatch:
jobs:
  approve_me:
    with:
      instructions: ""
      disallowLaunchByUser: false
      approvers: cbibin@cloudbees.com,usingh@cloudbees.com
      notifyAllEligibleUsers: false
    delegates: cloudbees-io/manual-approval/custom-job.yml@v1
    timeout-minutes: 10
 
  build:
    needs:
      - approve_me
    steps:
      - name: Say hello
        uses: docker://golang:1.20.3-alpine3.17
        shell: sh
        run: |
          echo "hello world"

  deploy:
    needs:
      - build
    environment: 7OctTestEnv
    steps:
      - name: Deploy step
        uses: docker://golang:1.20.3-alpine3.17
        shell: sh
        run: |
          echo "deploying to prod"
  
